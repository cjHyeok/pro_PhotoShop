<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ReviewMapper">

	<select id="ReviewList" parameterType="String" resultType="ReviewDTO">
		select review_id, review_date, review_content, user_name , product_id, review_like
		from t_review 
		where product_id = #{product_id}
		order by review_date desc
	</select>


	<insert id="reviewWriteAdd" parameterType="ReviewDTO">
		insert into
		t_review (review_id, user_name, product_id, review_content, review_date, review_like)
		values(review_seq.nextval, #{user_name}, #{product_id}, #{review_content}, sysdate, #{review_like})
	</insert>
</mapper>